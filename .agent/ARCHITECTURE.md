# Architecture

> Auto-generated by /map on 2026-09-19

## Overview

A Jamstack Point of Sale (POS) system for Koperasi Waserda PRIMKOPPOL NGAWI. Built with React (Vite) on the frontend, with a custom build script that generates static output and `_redirects` for deployment (likely Cloudflare Pages or Netlify). It uses RxDB and Supabase for state and database management, Firebase for applet configurations, and integrates with Google Drive for scheduled exports.

```text
┌─────────────────────────────────────────┐
│       Frontend (React/Vite app)         │
├─────────────────────────────────────────┤
│    Custom Build Layer (build.js)        │
├─────────────────────────────────────────┤
│    Offline-first Data (RxDB/Dexie)      │
├─────────────────────────────────────────┤
│  Remote Data (Supabase, Firebase, GDrive)
└─────────────────────────────────────────┘
```

## Components

### React Frontend
- **Purpose:** User interface for POS.
- **Location:** `src/App.jsx`, `src/main.jsx`, `index.html`
- **Dependencies:** React, React Router, TailwindCSS.

### Database Layer
- **Purpose:** Local state and sync with Supabase remote backend.
- **Location:** `src/db/index.js`, `kopos-db.js`
- **Dependencies:** RxDB, Supabase JS client.

### Background Export Workers
- **Purpose:** Scheduled data export to Google Drive.
- **Location:** `scheduled-export-manager.js`, `scheduled-export-worker.js`, `google-drive-service.js`
- **Dependencies:** Built-in web workers, external APIs.

### Build and Server
- **Purpose:** Custom static generation and preview server.
- **Location:** `build.js`, `server.js`
- **Dependencies:** Node.js, Express.

## Data Flow

1. User interaction updates local state in React components.
2. Data mutations go through RxDB/Dexie for local persistence (offline-first).
3. Background sync pushes data to Supabase remote database.
4. Scheduled workers periodically run exports to Google Drive.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Supabase | Database/API | Remote data sync and persistence |
| Firebase | API | Applet configuration |
| Google Drive | API | Scheduled data exports |

## Technical Debt

- [ ] Unused or misconfigured `base: '/'` in `vite.config.js`.
- [ ] Anomaly `;;` in `build.js` line 80 inside string literal.
- [ ] Custom `build.js` instead of standard `vite build`.

## Conventions

**Naming:** Kebab-case for JS files, PascalCase for React components.
**Structure:** Flat structure with most logic at the root, with React entry in `src/`.
**Testing:** No automated tests detected.
